{
  "name": "land-registry-contracts",
  "version": "1.0.0",
  "description": "Smart contracts pour le Registre Foncier Décentralisé sur CELO",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:alfajores": "hardhat run scripts/deploy.js --network alfajores",
    "deploy:celo": "hardhat run scripts/deploy.js --network celo",
    "verify:alfajores": "hardhat verify --network alfajores",
    "verify:celo": "hardhat verify --network celo",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "hardhat": "^2.19.0",
    "hardhat-gas-reporter": "^1.0.9",
    "@nomiclabs/hardhat-etherscan": "^3.1.7"
  },
  "dependencies": {
    "@celo/contractkit": "^5.2.0",
    "dotenv": "^16.3.1"
  }
}
